version: '3.5'
services:
  cmc:
    image: poller
    build: .
    environment:
      CONFIG: cmc-poller/cmc.yml
  coinbasepro:
    image: poller
    environment:
      CONFIG: exchange-poller/coinbasepro.yml
  bitstamp:
    image: poller
    environment:
      CONFIG: exchange-poller/bitstamp.yml
  kraken:
    image: poller
    environment:
      CONFIG: exchange-poller/kraken.yml
  poloniex:
    image: poller
    environment:
      CONFIG: exchange-poller/poloniex.yml
  binance:
    image: poller
    environment:
      CONFIG: exchange-poller/binance.yml
  oanda:
    image: poller
    environment:
      CONFIG: exchange-poller/oanda.yml
  # bittrex:
  #   image: poller
  #   environment:
  #     CONFIG: exchange-poller/bittrex.yml
networks:
  default:
    external:
      name: crypto
